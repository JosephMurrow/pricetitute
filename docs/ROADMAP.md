# Pricetitute — роадмап

Правила и функционал — в [SPEC.md](./SPEC.md). Здесь только план работ:
что делаем, в каком порядке и что считается готовым.

## Порядок и зависимости

```
Э0 Каркас
 └─ Э1 Аккаунты ──┐
 └─ Э2 Вопросы ───┴─ Э3 Игровой движок ─ Э4 Реалтайм ─ Э5 Интерфейс игры
                                                        ├─ Э6 Общая комната
                                                        └─ Э7 Приватные комнаты
                                                             └─ Э8 Полировка и деплой
```

Э1 и Э2 независимы, делаются параллельно. Э3 и Э5 идут внахлёст: как только
машина состояний отдаёт события, интерфейс собирается против реального контракта,
а не против макета.

---

## Этап 0 — Каркас проекта

- [x] `create-next-app`: Next.js 16 (App Router) + TypeScript + Tailwind 4
- [x] ESLint + Prettier, строгий `tsconfig` (`noUncheckedIndexedAccess` и др.)
- [x] `docker-compose.yml` с PostgreSQL 17 для локальной разработки
- [x] Prisma 7: схема, `prisma.config.ts`, адаптер `@prisma/adapter-pg`,
      миграция `init`, `.env.example`
- [x] Кастомный сервер `server.ts`: Next + Socket.IO в одном процессе,
      upgrade-запросы разведены между Socket.IO и Next HMR
- [x] Базовый layout, тёмная тема, палитра, кириллический сабсет шрифтов
- [x] `.gitignore`, README, первый коммит

**Готово:** `npm run dev` поднимает приложение и сокет-сервер на одном порту,
`prisma migrate dev` накатывает схему, `npm run build`, `lint` и `typecheck`
проходят чисто.

---

## Этап 1 — Аккаунты

- [x] Схема `User`: `login`, `passwordHash`, `nickname`, `avatarId`, `adultConfirmedAt`, `createdAt` — заведена в этапе 0 вместе с первой миграцией
- [x] Хеширование паролей (argon2), сессия — подписанный JWT в httpOnly cookie
- [x] Регистрация: логин 3–20 символов, пароль от 8, ник отдельным полем, галочка 18+
- [x] Вход, выход, защита роутов через `proxy.ts`
- [x] **Пул из 30 аватаров** — 8 зверей (кот, пёс, ёж, ящерка, лиса, лягушка,
      сова, енот) в цветовых вариантах, часть с кепкой, очками или бабочкой.
      Рисуются инлайновым SVG, файлов на диске нет. Выдача случайного при
      регистрации.
- [x] Страница профиля: смена ника, перевыбор аватара из пула
- [x] Валидация форм и человеческие тексты ошибок

**Готово:** регистрация, вход, выход и смена аватара работают; логин уникален
без учёта регистра; неверный пароль и несуществующий логин дают одинаковый
текст ошибки; введённые значения переживают неудачную отправку формы.
Восстановления пароля нет — это решённый компромисс, не задача.

---

## Этап 2 — Пул вопросов

- [ ] Схема `Question`: `text`, `adult`, `active`
- [ ] Схема истории показов: глобальная очередь для общей комнаты + отдельная
      история для каждой приватной
- [ ] **Написать стартовый пул ~500 вопросов** в формате «за какую сумму ты бы…».
      Пропорция: **50% бытовые и неловкие (~250), 30% социально рискованные (~150),
      20% 18+ (~100)**. Пишется партиями по 100 штук, каждая партия — отдельный
      файл сида, чтобы можно было вычитывать и править по частям
- [ ] Seed-скрипт заливки пула в БД
- [ ] Логика очереди: перемешивание, выдача с головы, возврат сгоревшего вопроса
      в нижнюю треть на случайную позицию, повторное перемешивание при исчерпании
- [ ] Тесты на очередь: без повторов до исчерпания, сгоревший не всплывает сразу

**Готово, когда:** сервер по запросу отдаёт следующий вопрос с учётом истории комнаты.

---

## Этап 3 — Игровой движок (сервер)

Ядро проекта. Пишется отдельно от транспорта, чтобы покрывалось тестами.

- [ ] Модель состояния комнаты в памяти: игроки, круг ходов, текущая фаза, дедлайн
- [ ] Машина состояний: `READY 20с → HOST_ANSWER 15с → BETTING 5мин → REVEAL 30с`
- [ ] Серверные таймеры, наружу отдаётся абсолютный `deadline` (unix ms)
- [ ] Переход хода по кругу, пропуск отвалившихся, сгорание вопроса
- [ ] Приём ставок: только один раз, изменение запрещено, досрочное закрытие
      фазы когда поставили все
- [ ] **Подсчёт очков**: логарифмическая близость, спец-случаи «Бесплатно» и
      «Ни за какие деньги», ничьи, ведущий без очков
- [ ] Пауза комнаты при <2 активных игроков
- [ ] Персист в БД: раунды, ставки, начисленные очки
- [ ] Юнит-тесты: формула близости, все спец-случаи с ∞ и 0, ничьи, каждый
      переход фаз, сгорание на обоих таймерах

**Готово, когда:** движок прогоняет полный цикл раундов в тестах, без сети и UI.

---

## Этап 4 — Реалтайм-слой

- [ ] Аутентификация сокета по cookie сессии
- [ ] Комнаты Socket.IO, подписка/отписка
- [ ] Контракт событий:
      `room:state`, `round:phase`, `round:question`, `bet:placed`,
      `round:reveal`, `chat:message`, `player:joined/left`
- [ ] Реконнект: восстановление полного состояния комнаты одним снапшотом
- [ ] Рейт-лимиты на ставки и чат, серверная валидация любого ввода
- [ ] Отдельная проверка: клиент не получает ответ ведущего и чужие ставки
      до фазы REVEAL

**Готово, когда:** два браузера играют раунд друг с другом, обновление страницы
не ломает состояние.

---

## Этап 5 — Интерфейс игры

- [ ] Экран комнаты: вопрос, индикатор фазы, таймер, список игроков со статусами
      («думает» / «поставил» / «отключился»)
- [ ] Экран ведущего: кнопка «Прочитал» с обратным отсчётом 20с, затем ввод
      своей суммы за 15с
- [ ] **Ввод ставки**: числовое поле с форматированием разрядов, быстрые пресеты
      (1к / 10к / 100к / 1млн), кнопки «Бесплатно» и «Ни за какие деньги»
- [ ] Экран вскрытия: все ставки и ответ ведущего разом, подсветка победителя,
      наглядная шкала «кто насколько промахнулся»
- [ ] Таблица очков комнаты
- [ ] Чат: история 100 сообщений, лимит 300 символов
- [ ] Адаптив: десктоп и мобильная вёрстка
- [ ] Состояния ожидания: «ждём второго игрока», «ждём ведущего», пауза

**Готово, когда:** играть можно с телефона и с десктопа, ничего не разъезжается.

---

## Этап 6 — Общая комната

- [ ] Автоматический вход в единственную общую комнату
- [ ] Круг ходов в порядке подключения, новый игрок в конец круга
- [ ] Начисление очков в глобальный рейтинг
- [ ] Страница рейтинга: за всё время и за текущую неделю, топ-100 + своя строка
      (ник, аватар, очки, сыгранные раунды)
- [ ] Поведение при наплыве: проверить, что круг ходов не разваливается

**Готово, когда:** очки из общей комнаты попадают в обе таблицы рейтинга.

---

## Этап 7 — Приватные комнаты

- [ ] Создание комнаты, генерация кода, страница `/r/<code>`
- [ ] Инвайт-ссылка, вход только по аккаунту, редирект незалогиненного на
      регистрацию и обратно в комнату
- [ ] Настройки хоста: длительность фазы ставок, 18+ вкл/выкл, условие конца
      игры (N раундов / до N очков / бесконечно)
- [ ] Права хоста: старт игры, кик игрока
- [ ] Счёт изолирован от глобального рейтинга
- [ ] Финальный экран с победителем при выполнении условия конца
- [ ] Удаление комнаты через 30 минут после выхода последнего игрока или по
      кнопке хоста, ссылка умирает вместе с комнатой

**Готово, когда:** компания заходит по одной ссылке и играет партию до конца.

---

## Этап 8 — Полировка и запуск

- [ ] Лендинг с правилами и возрастной плашкой
- [ ] Пустые состояния, экраны ошибок, поведение при обрыве сети
- [ ] Звуки и анимации вскрытия — по желанию, не блокирует запуск
- [ ] Логирование и отлов ошибок на сервере
- [ ] Деплой (Railway / Render / VPS), миграции Prisma, бэкап БД
- [ ] Проверка под нагрузкой: много игроков в одной общей комнате

---

## Отложено осознанно

Из раздела компромиссов спеки — делаем, только если упрёмся:

- админка вопросов и пользовательские предложения вопросов;
- блокировка чата в фазе ставок против сговора в общей комнате;
- капча или обязательный email против мультиаккаунтов в рейтинге;
- параллельные столы в общей комнате, если ожидание своего хода станет долгим.
